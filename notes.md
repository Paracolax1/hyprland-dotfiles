wallust on AUR won't build for some reason, investigate later, install with cargo install wallust for now.

vicinae doesn't support aarch64 (only arm 32b archs), try force build manually?

waytrogen doesnt work

hyprexpo installs but fails to initialise (bind config disabled for now)

in installer set default wallpaper for theme-sync to work

battery charge percentage 80% with health check 100% 1-2months
also fix that werid kernel warning about soc and battery

chromium sync flags

hyprshutdown and hyprsysteminfo don't install (even though they should)